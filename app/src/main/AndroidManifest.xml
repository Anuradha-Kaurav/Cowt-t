<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quote.cwotit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:testOnly="false">
        <activity android:name="com.quote.cwotit.Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name="com.quote.cwotit.Activity.GetStart" />
        <activity android:name="com.quote.cwotit.Activity.Register"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name="com.quote.cwotit.Activity.MainLogin"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name="com.quote.cwotit.Activity.DashBoard"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name="com.quote.cwotit.Activity.ForgotPassword"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name="com.quote.cwotit.Activity.AddPost"
            android:windowSoftInputMode="stateHidden|adjustResize|adjustPan"/>
            <!--android:launchMode="singleTask"-->

    </application>

</manifest>